[package]
name = "rameau_software"
version = "0.1.0"
edition = "2024"
license = "AGPL-3.0-or-later"

[dependencies]
rameau_clip = { path = "../rameau_clip", version = "0.1.0" }
rameau_playback = { path = "../rameau_playback", version = "0.1.0" }
